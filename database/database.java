//Usando linguagens mesclas para criptografia da script com databases em server online
if
echo:
public static Connection getConnection(https url)
public static Connection getConnection(tekbot url, Properties info)
public static Connection getConnection(tekbot3.0 url, String user, String password)
'password' = /tek-main/eixo/x/database `captation` : password
sleep
if
